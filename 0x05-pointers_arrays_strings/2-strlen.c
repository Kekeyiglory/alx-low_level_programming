#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: string pointer
 * Return: return length of the string
 */
int _strlen(char *s)
{
	int a = 0;

	for (; *s != '\0', a++);
	{
		a++;
	}

	return (a);
}
